spring:
  application:
    name: mp-boot
  datasource:
    url: jdbc:mysql://121.36.33.154:33600/mp?serverTimezone=UTC&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
logging:
  level:
    cn.yuanyu.mp.mapper: debug

mybatis-plus:
  type-enums-package: cn.yuanyu.mp.enums # 枚举包扫描
  global-config:
    db-config:
      logic-delete-value: 1 # 逻辑已删除值(默认为1)
      logic-not-delete-value: 0 # 逻辑未删除值(默认为0)